import type { EdgeTypes, FitViewOptions, NodeTypes, ProOptions } from 'reactflow'

export const STABLE_NODE_TYPES: NodeTypes = {}
export const STABLE_EDGE_TYPES: EdgeTypes = {}

export const DEFAULT_PRO_OPTIONS: ProOptions = {
  hideAttribution: true,
}

export const DEFAULT_FIT_OPTIONS: FitViewOptions = {
  padding: 0.25,
  maxZoom: 1.15,
}
